{
  "name": "dm005-courseinfra",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-registry.mjs",
    "check": "npm run validate && node --check scripts/deploy-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
