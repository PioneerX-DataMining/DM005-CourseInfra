{
  "name": "dm005-courseinfra",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-registry.mjs",
    "render": "node scripts/render-gateway.mjs",
    "check": "npm run validate && npm run render && node --check gateway/middleware.js",
    "build": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
